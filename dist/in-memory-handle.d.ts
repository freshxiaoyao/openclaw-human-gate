/**
 * Deprecated placeholder retained for package compatibility.
 *
 * Human Gate no longer uses process-global in-memory session state. Approval
 * grants and approval windows are stored in OpenClaw session extensions and
 * explicitly keyed by the active session.
 */
export {};
//# sourceMappingURL=in-memory-handle.d.ts.map